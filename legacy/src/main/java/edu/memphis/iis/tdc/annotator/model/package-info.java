/**
 * Our model class for a tagging taxonomy (which includes a DialogAct) and
 * the transcripts (which include individual Utterances)
 *
 *  <p>These model classes include annotations for XML read/write via the
 *  Simple XML library
 */
package edu.memphis.iis.tdc.annotator.model;
