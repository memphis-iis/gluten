/**
 * This laughably small package is for our data service layer and
 * implementations.  Currently that's our file system based service
 * for Transcripts.
 * 
 * To add insult to injury, most of the actual persistance functionality
 * is provided via the Simple XML library, so you need to see the model
 * classes for the annotations mapping them to XML
 */
package edu.memphis.iis.tdc.annotator.data;